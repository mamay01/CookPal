import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BicolexpressingredPage } from './bicolexpressingred.page';

describe('BicolexpressingredPage', () => {
  let component: BicolexpressingredPage;
  let fixture: ComponentFixture<BicolexpressingredPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(BicolexpressingredPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
