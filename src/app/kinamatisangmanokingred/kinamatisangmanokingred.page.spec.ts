import { ComponentFixture, TestBed } from '@angular/core/testing';
import { KinamatisangmanokingredPage } from './kinamatisangmanokingred.page';

describe('KinamatisangmanokingredPage', () => {
  let component: KinamatisangmanokingredPage;
  let fixture: ComponentFixture<KinamatisangmanokingredPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(KinamatisangmanokingredPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
