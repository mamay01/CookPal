import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PorkmonggocrispgalungongingredPage } from './porkmonggocrispgalungongingred.page';

describe('PorkmonggocrispgalungongingredPage', () => {
  let component: PorkmonggocrispgalungongingredPage;
  let fixture: ComponentFixture<PorkmonggocrispgalungongingredPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(PorkmonggocrispgalungongingredPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
