import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PorkmonggocrispgalungonginstPage } from './porkmonggocrispgalungonginst.page';

describe('PorkmonggocrispgalungonginstPage', () => {
  let component: PorkmonggocrispgalungonginstPage;
  let fixture: ComponentFixture<PorkmonggocrispgalungonginstPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(PorkmonggocrispgalungonginstPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
