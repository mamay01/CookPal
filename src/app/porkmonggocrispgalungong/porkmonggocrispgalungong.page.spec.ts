import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PorkmonggocrispgalungongPage } from './porkmonggocrispgalungong.page';

describe('PorkmonggocrispgalungongPage', () => {
  let component: PorkmonggocrispgalungongPage;
  let fixture: ComponentFixture<PorkmonggocrispgalungongPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(PorkmonggocrispgalungongPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
